using System;

namespace TheQuestion
{
    class Program
    {
        static void Main(string[] args)
        {
            int theAnswer = '*'; // the wildcard character for matching 'anything'
            Console.Write("The answer to life, the universe and everything is: ");
            Console.WriteLine(theAnswer);
        }
    }
}
